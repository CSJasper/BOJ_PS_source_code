#include <iostream>
#include <ctype.h>
#include <cstring>

#define MAX_STR_LENGTH 1000001
unsigned char _itoa(const int ch);
int main(void)
{
    unsigned char str[MAX_STR_LENGTH] = {0,};
    fgets(str, MAX_STR_LENGTH, stdin);
    for(int i = 0 ; i < strlen(str) ; i++)
    {
        str[i] = 
    }
    
    return 0;
}
unsigned char _itoa(const int ch)
{
    
}
